# ---- Core runtime -------------------------------------------------------
PySide6==6.9.1            # Windows desktop UI (Qt 6)
websockets==15.0.1        # ElevenLabs realtime STT transport
openai>=1.99,<3           # OpenAI Responses API (streaming answer generation)
numpy>=1.26,<3            # PCM conversion / polyphase resampling
keyring>=25.4             # Windows Credential Manager for API keys
pypdf>=5.1                # CV / job-description PDF extraction
python-docx>=1.1          # CV / job-description DOCX extraction

# ---- Windows audio capture (WASAPI loopback) ---------------------------
# PyAudioWPatch is a Windows-only PyAudio fork that exposes WASAPI loopback
# devices, which is how we capture "what Windows is playing".
PyAudioWPatch==0.2.12.7 ; sys_platform == "win32"
